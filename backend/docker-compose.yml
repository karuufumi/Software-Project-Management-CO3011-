services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: library-management-api
    ports:
      - "5000:5000"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5000
      - ConnectionStrings__DefaultConnection=Host=trolley.proxy.rlwy.net;Port=32928;Database=railway;Username=postgres;Password=IYmDYGOIkvRHVMpMLHBOwIWKHuyOIXNd
      # Persist database
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - library-network

networks:
  library-network:
    driver: bridge